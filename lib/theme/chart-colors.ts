// Reuses the same semantic tokens as the rest of the UI rather than a separate
// hardcoded palette, so charts retheme along with everything else.
export const chartColors = [
  'var(--color-primary)',
  'var(--color-success)',
  'var(--color-warning)',
  'var(--color-error)',
  'var(--color-accent)',
  'var(--color-secondary)',
]
